[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "deepspec-mts-fork"
version = "0.1.0"
description = "Pinned DeepSpec fork used by MTS integration"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["deepspec*"]

